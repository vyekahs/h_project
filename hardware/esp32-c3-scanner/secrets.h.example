// secrets.h.example — 이 파일을 복사해서 같은 폴더에 secrets.h로 저장하고 실제 값을 채우세요.
// secrets.h는 .gitignore에 등록돼 있어 커밋되지 않습니다.
#pragma once

const char* WIFI_SSID = "";
const char* WIFI_PASS = "";
const char* SCANNER_API_KEY = "";
